# Copier vers .env pour le dev local : cp .env.example .env
NEXT_PUBLIC_API_URL=http://localhost:9191
NEXT_PUBLIC_APP_ENV=development
# Optionnel — Route Handlers serveur si l’API n’est pas joignable via l’URL publique
# API_URL=http://localhost:9191
